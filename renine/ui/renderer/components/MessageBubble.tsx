/**
 * MessageBubble — Individual message display component.
 * Placeholder for Phase 1 — logic is inline in ChatWindow.
 * Will be extracted into a proper React component in future phases.
 * @module MessageBubble
 */
export {};
